package api.carpooling.application.dto.user;

/**
 * DTO for creating a user.
 * <p>
 * Currently empty, can be extended in the future with user creation fields.
 */
public record CreateUserRequest() {
}
